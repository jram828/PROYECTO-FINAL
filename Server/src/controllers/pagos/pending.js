 import { models } from "../../DB.js";
 const { Pagos } = models;
const pending = async (body) => {
  const newPago = "";
  console.log("Body pending controller: ", body);
  return body;
};

export {
  pending,
};
