const { Pagos } = require("../../DB");

const success = async (password, email) => {
  const newPago = "";
  return {
    status: completed,
    pago: newPago,
  };
};

module.exports = {
  success,
};
