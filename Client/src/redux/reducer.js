

let initialState = {
  usuario: {},
  isAuthenticated: false,
};

const rootReducer = (state = initialState, action) => {
console.log('Stado inicial:', state.usuario)
  
};

export default rootReducer;
